# Attestation Tools

Набор из двух утилит для работы с сервисом **attestation.app**:

---

## 📌 Описание

### **1. dump.py**
- Авторизуется в `attestation.app` с помощью логина и пароля из `.env`.
- Загружает список устройств.
- Для каждого устройства скачивает полную историю аттестаций.
- Сохраняет результат в файл **attestation_dump.json**.

### **2. diff.py**
- Анализирует содержимое `attestation_dump.json`.
- Сравнивает последовательные записи истории.
- Определяет, какие поля изменились.
- Выводит список изменений с меткой времени.

---

## 📦 Требования

Файл **requirements.txt** должен содержать:

```
requests
python-dotenv
```

---

## ⚙️ Подготовка

Создайте файл `.env` в рабочей директории:

```
ATTEST_USERNAME=your_login
ATTEST_PASSWORD=your_password
```

Файл обязателен — без него `dump.py` не начнёт работу.

---

## ▶️ Запуск

### 1. Выгрузить данные из attestation.app:

```
python dump.py
```

После выполнения появится файл:

```
attestation_dump.json
```

### 2. Проанализировать изменения:

```
python diff.py
```

---

## 🧩 Настройки diff.py — поля, исключаемые из сравнения

Скрипт не учитывает ряд полей, значения которых меняются всегда или не влияют на результат проверки.

Редактировать можно в переменной:

```python
IGNORE_FIELDS = {
    "id",
    "offsetId",
    "verifiedTimeFirst",
    "verifiedTimeLast",
    "timestamp",
    "time",
    "_id",
    "verifiedBootHash",
    "osPatchLevel",
    "vendorPatchLevel",
    "bootPatchLevel"
}
```

Вы можете добавлять или удалять элементы в этом наборе.

Например, если хотите сравнивать `osPatchLevel`, просто удалите эту строку из списка.

---

## 📁 Результаты

- **attestation_dump.json** — структура:
  ```json
  {
    "fingerprint1": {
      "device": {...},
      "history": [...]
    },
    "fingerprint2": { ... }
  }
  ```

- `diff.py` выводит:
  - дату изменения
  - список полей, в которых различаются две соседние записи
  - старое и новое значение

---

## ✔️ Пример вывода diff.py

```
[Изменение] 2024-01-21 14:33:10
Изменились поля:
  - verifiedBootState: green → orange
  - deviceLocked: true → false
```

---
